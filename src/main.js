// @ts-check
// Planning Menetrey - ESM bundle entry point.
// J1 scaffold: smoke-test only. Real app code arrives in J2+.

console.log('[planning] bundle loaded, version=0.0.0-refonte');

const root = document.getElementById('app');
if (root) {
  root.textContent = 'Planning Menetrey - ESM bundle scaffold (J1). Aucun code applicatif chargé.';
}
